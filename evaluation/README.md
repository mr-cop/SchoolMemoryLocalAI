# Evaluation

No private or product-specific evaluation corpus is stored here.

This public repository may eventually contain generic runtime smoke tests that use synthetic, non-sensitive text solely to verify that a packaged model can load and return structurally valid text.

Behavioural quality, domain suitability and application-specific regression tests belong in a private repository.
